export declare class PageService {
    put(user: string, name: string, url: string): Promise<import("../standard").CustomResponse>;
    get(user: string, name: string): Promise<import("../standard").CustomResponse>;
}
