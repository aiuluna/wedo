import { Bridge } from "@wedo/meta"

export const ListRender = ({
  bridge,
  children,
  childrenProps,
  gapIndex
}: {
  bridge: Bridge,
  children?: JSX.Element,
  childrenProps?: any,
  gapIndex?: number
}) => {
  return <>
  </>
}